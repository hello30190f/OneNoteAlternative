


# create websocket stream that transfar bin data between dataserver and frontend. (for each file)







Settings = None
def init(RuntimeSettings:dict) -> None:
    global Settings
    Settings = RuntimeSettings
